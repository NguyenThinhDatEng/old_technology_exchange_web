{
  "kind": "collectionType",
  "collectionName": "carings",
  "info": {
    "name": "caring"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "product": {
      "via": "carings",
      "model": "product"
    },
    "customer": {
      "via": "carings",
      "model": "customer"
    }
  }
}
